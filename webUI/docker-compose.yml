services:
  web:
    build:
      context: .
      target: ${DOCKER_TARGET:-runner}
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - ${ENV_FILE:-.env.local}
    environment:
      NODE_ENV: ${NODE_ENV:-production}
